# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change_me
DB_NAME=requisition_db

# JWT secret used by backend for signing tokens (use strong random value)
JWT_SECRET=change_this_to_a_strong_secret

# Server port
PORT=3001
